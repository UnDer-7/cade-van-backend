logging:
  level:
    ROOT: info
spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/CADE_VAN
    password: docker
    username: docker
  jpa:
    database: postgresql
    show-sql: true
server:
  port: 8080
  protocol: http